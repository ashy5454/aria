# CMP Research Wiki

The agent writes here after every experiment. This is its long-term memory.
Organized by topic so the agent can search before proposing redundant experiments.

## Format

Each entry: one file per topic. Agent reads the relevant files before proposing.
Agent writes a new entry after each experiment — what it tried, what happened, why.
